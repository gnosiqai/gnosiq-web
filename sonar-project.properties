sonar.projectKey=gnosiqai_gnosiq-web
sonar.organization=gnosiqai
sonar.sources=app,components,lib,hooks
sonar.exclusions=**/*.test.ts,**/*.test.tsx,**/*.spec.ts,**/*.spec.tsx,**/__tests__/**,**/node_modules/**,**/.next/**
sonar.javascript.lcov.reportPaths=coverage/lcov.info

# Metadata routes do Next.js (declarativas, sem lógica de negócio).
# Exclusão pontual — NÃO adicionar paths sem review. Quality Gate
# permanece 80% para todo código de negócio (pipeline M2 incluído).
sonar.coverage.exclusions=app/robots.ts,app/sitemap.ts

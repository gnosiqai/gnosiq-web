/**
 * CONTROLE NEGATIVO (arquivo descartável de prova adversarial).
 * O padrão banido aparece aqui SÓ em comentário: GoogleAuth.getClient()
 * e o módulo google-auth-library. Comentário não executa.
 * Esperado: Conformance Asserts VERDE.
 */
// import { GoogleAuth } from 'google-auth-library'
export const controle = true
